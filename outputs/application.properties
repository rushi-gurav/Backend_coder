spring.datasource.url=jdbc:mysql://localhost:3306/userdatabase
spring.datasource.username=root
spring.datasource.password=root
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5Dialect

This setup now allows the `POST /submit` endpoint to accept JSON data from the frontend and save it to the MySQL database. The frontend HTML form will POST data to this endpoint.

### Frontend Code
The provided frontend code is a simple HTML form that submits data to your Spring Boot backend. Ensure that the backend service is running and accessible when you submit the form.
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Form Page</title>
</head>
<body>
  <h1>Submit Your Details</h1>
  <form action="/submit" method="POST">
    <label for="username">Username:</label>
    <input type="text" id="username" name="username" required>
    <br><br>

    <label for="email">Email:</label>
    <input type="email" id="email" name="email" required>
    <br><br>

    <label for="password">Password:</label>
    <input type="password" id="password" name="password" required>
    <br><br>

    <label for="comments">Comments:</label>
    <textarea id="comments" name="comments" rows="4" cols="50"></textarea>
    <br><br>

    <button type="submit">Submit</button>
  </form>
</body>
</html>

If you want to submit the form data as JSON, you might need to use JavaScript (e.g., Fetch API or Axios) to modify the form submission behavior from the frontend. Let me know if you need that as well!